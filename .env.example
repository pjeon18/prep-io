# Optional: enables the LLM-driven crowd personas in dev.
# The Vite dev proxy injects this server-side at /api/crowd — the key never
# reaches browser code. Without it, the crowd is scripted (silent fallback).
ANTHROPIC_API_KEY=
